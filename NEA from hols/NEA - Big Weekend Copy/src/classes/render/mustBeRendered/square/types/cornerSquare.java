package classes.render.mustBeRendered.square.types;

import classes.render.mustBeRendered.square.Square;

public class cornerSquare extends Square {

    public cornerSquare(String fn) {
        super(fn);
    }
}
